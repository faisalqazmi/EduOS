// NOTE: This is a temporary placeholder to prevent TypeScript errors.
// Once your Supabase project is created, overwrite this file by running:
// npx supabase gen types typescript --project-id "YOUR_PROJECT_REF" > src/types/database.ts

export type Database = any;
export type Tables<T extends string> = any;
export type Enums<T extends string> = any;
